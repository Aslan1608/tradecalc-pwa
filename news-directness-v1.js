(()=>{'use strict';
const ALIASES={GOOG:['Alphabet','Google'],GOOGL:['Alphabet','Google'],META:['Meta','Facebook'],NFLX:['Netflix'],AMZN:['Amazon'],MSFT:['Microsoft'],AAPL:['Apple'],NVDA:['Nvidia','NVIDIA'],AVGO:['Broadcom'],AMD:['AMD','Advanced Micro Devices'],ADBE:['Adobe'],CRM:['Salesforce'],COST:['Costco'],JPM:['JPMorgan','JP Morgan'],V:['Visa'],MA:['Mastercard'],ASML:['ASML']};
let mode='direct',timer=0,observer=null,lastTicker='';
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function norm(v){return String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim()}
function selected(){const s=document.getElementById('siSymbol');if(!s)return null;const ticker=String(s.value||'').trim().toUpperCase().replace(/\.DE$/,'');const text=String(s.options[s.selectedIndex]?.textContent||'');const company=(text.includes('—')?text.split('—').slice(1).join('—'):text).trim();return{ticker,company,aliases:[company,ticker,...(ALIASES[ticker]||[])].filter(Boolean)}}
function directTitle(title,info){const t=' '+norm(title)+' ';for(const alias of info.aliases){const a=norm(alias);if(a.length<3)continue;if(t.includes(' '+a+' '))return true}return false}
function parseScore(story,index){const nodes=story.querySelectorAll('.v37-score b');const raw=nodes[index]?.textContent||'';const m=raw.match(/(\d+)/);return m?Number(m[1]):0}
function direction(story){for(const x of ['bullish','bearish','ambivalent','neutral'])if(story.classList.contains(x))return x;return'neutral'}
function computePulse(stories){const eligible=stories.filter(s=>!s.classList.contains('v37-opinion')).map(s=>({s,impact:parseScore(s,0),confidence:parseScore(s,1),dir:direction(s)})).filter(x=>x.impact>=25).sort((a,b)=>b.impact-a.impact).slice(0,5);let num=0,den=0;for(const x of eligible){const sign=x.dir==='bullish'?1:x.dir==='bearish'?-1:0,w=Math.sqrt(x.impact)*(x.confidence/100);num+=sign*w;den+=w}return{score:Math.round(Math.max(0,Math.min(100,50+50*(den?num/den:0)))),used:eligible}}
function style(){if(document.getElementById('newsDirectnessStyles'))return;const s=document.createElement('style');s.id='newsDirectnessStyles';s.textContent=`.snd-filter{display:grid;grid-template-columns:repeat(3,1fr);gap:6px;margin:10px 0}.snd-filter button{min-height:38px;border:1px solid #343c48;border-radius:10px;background:#0e1319;color:#8c95a3;font-size:9px;font-weight:900}.snd-filter button.active{background:#fff;color:#050607;border-color:#fff}.v37-story.senseis-context{border-style:dashed}.v37-badge.snd-direct{color:#30d158;border-color:rgba(48,209,88,.4)}.v37-badge.snd-context{color:#8c95a3}.snd-note{margin:7px 0;color:#8c95a3;font-size:8px;line-height:1.4}`;document.head.appendChild(s)}
function patch(){clearTimeout(timer);timer=setTimeout(()=>{const info=selected(),box=document.getElementById('siNews');if(!info||!box)return;const shell=box.querySelector('.sni-v37-shell');if(!shell)return;if(lastTicker!==info.ticker){mode='direct';lastTicker=info.ticker}const list=box.querySelector('.v37-list'),stories=[...box.querySelectorAll('.v37-list .v37-story')];if(!list||!stories.length)return;let direct=0,context=0;for(const story of stories){const title=story.querySelector('h4')?.textContent||'';const isDirect=directTitle(title,info);story.dataset.senseisRelation=isDirect?'direct':'context';story.classList.toggle('senseis-direct',isDirect);story.classList.toggle('senseis-context',!isDirect);direct+=isDirect?1:0;context+=isDirect?0:1;let badge=story.querySelector('.snd-relation');if(!badge){badge=document.createElement('span');badge.className='v37-badge snd-relation';story.querySelector('.v37-badges')?.appendChild(badge)}badge.classList.toggle('snd-direct',isDirect);badge.classList.toggle('snd-context',!isDirect);badge.textContent=isDirect?'🎯 DIREKT':'↔ UMFELD';story.style.display=mode==='all'||(mode==='direct'&&isDirect)||(mode==='context'&&!isDirect)?'':'none'}let filter=box.querySelector('.snd-filter');if(!filter){filter=document.createElement('div');filter.className='snd-filter';list.insertAdjacentElement('beforebegin',filter)}filter.innerHTML=`<button data-snd="direct">🎯 Direkt ${direct}</button><button data-snd="context">↔ Umfeld ${context}</button><button data-snd="all">Alle ${direct+context}</button>`;filter.querySelectorAll('button').forEach(b=>{b.classList.toggle('active',b.dataset.snd===mode);b.onclick=()=>{mode=b.dataset.snd;patch()}});let note=box.querySelector('.snd-note');if(!note){note=document.createElement('div');note.className='snd-note';filter.insertAdjacentElement('afterend',note)}note.textContent='News-Pulse und Unternehmenslage basieren standardmäßig nur auf direkt zugeordneten Storys. Umfeld bleibt separat sichtbar.';const directStories=stories.filter(s=>s.dataset.senseisRelation==='direct');const p=computePulse(directStories);const ring=box.querySelector('.v37-ring'),strong=ring?.querySelector('strong');if(ring)ring.style.setProperty('--score',String(p.score));if(strong)strong.textContent=String(p.score);const pulseTitle=box.querySelector('.v37-pulse h4');if(pulseTitle)pulseTitle.textContent=p.score>=62?'🟢 DIREKTER STORY-PULSE POSITIV':p.score<=38?'🔴 DIREKTER STORY-PULSE NEGATIV':'⚪ DIREKTER STORY-PULSE AUSGEGLICHEN';const pulseText=box.querySelector('.v37-pulse p');if(pulseText)pulseText.textContent='Richtung nur aus direkt dem Unternehmen zugeordneten, relevanten Story-Clustern — keine Kursprognose.';const stats=box.querySelectorAll('.v37-stat');if(stats[0])stats[0].innerHTML=`<span>DIREKTE STORYS</span><strong>${direct}</strong>`;if(stats[3])stats[3].innerHTML=`<span>DIREKTE PULSE-BASIS</span><strong>${p.used.length} relevant</strong>`;const counts={bullish:0,bearish:0,neutral:0,ambivalent:0};directStories.filter(s=>!s.classList.contains('v37-opinion')).forEach(s=>counts[direction(s)]++);const countNodes=box.querySelectorAll('.v37-counts span');const vals=[['bullish','🟢'],['neutral','⚪'],['ambivalent','🟠'],['bearish','🔴']];countNodes.forEach((node,i)=>{const [k,icon]=vals[i]||[];if(k)node.textContent=`${icon} ${counts[k]} ${k}`});window.dispatchEvent(new CustomEvent('senseis-news-directness-updated',{detail:{ticker:info.ticker,direct,context,pulse:p.score}}));},80)}
function boot(){style();patch();document.addEventListener('change',e=>{if(e.target?.id==='siSymbol'){lastTicker='';patch()}});observer=new MutationObserver(()=>patch());observer.observe(document.body,{subtree:true,childList:true})}
if(typeof window!=='undefined')window.SenSeiSNewsDirectness={directTitle,computePulse};
if(typeof module!=='undefined'&&module.exports)module.exports={directTitle,computePulse};
if(typeof document!=='undefined'){if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()}
})();
